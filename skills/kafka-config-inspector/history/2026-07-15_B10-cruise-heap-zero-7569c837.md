# B10: cruise heap = 0 — ожидаем 422, получили 202 (валидатор не отбивает 0)

- **Cluster**: `7569c837-37ba-4041-9046-92329683237e` (test-resize)
- **Operation**: `0abdff46-c7ea-4d1c-86eb-7148d50c627a`
- **Запрос**: `/tmp/modify_B10.json` — `cruiseControl.jvmHeapSizeMb: 0` (ожидание 422)
- **HTTP**: **202** (вместо ожидаемого 422)

## Что произошло

Валидатор `KafkaClusterModificationValidator` не отбивает `cruiseControl.jvmHeapSizeMb=0`. Операция создаётся и workflow стартует.

В `cluster_params` версии записано `jvmHeapSizeMb: 0`.

## Temporal workflow

- `modifyKafkaCluster` 0abdff46 — RUNNING (workflow шёл нормально до cruise-фазы)
- `modifyBroker` — COMPLETED
- `modifyController` — COMPLETED
- `modifyCruise` / `updateCruiseConfig` — RUNNING (висит, т.к. одна-cloud reload недоступен)

## PMS-результат

Cruise host `1.cruise.test-resize-mdbdev-kafka.dc.one-infra.ru`:
- `kafka.cruisecontrol.sysconfig` → `KAFKA_HEAP_OPTS="-Xms4096m -Xmx4096m"` (не изменился)

То есть `jvmHeapSizeMb=0` **не применяется** как 0m — возможно:
- Workflow игнорирует 0 (no-op)
- Или `updateCruiseConfig` ещё не дошёл до записи PMS

## Выводы

1. **B10-гипотеза не подтвердилась**: `cruiseControl.jvmHeapSizeMb=0` не вызывает 422 от валидатора mdb-data.
2. Если нужен валидатор, который отбивает 0/негативные значения cruise heap — его нет в текущем коде mdb-data. Возможно валидация есть в mdb-processing, но workflow туда доходит.
3. `jvmHeapSizeMb=0` в cluster_params записывается, но в PMS не применяется (по крайней мере до завершения `updateCruiseConfig`).
