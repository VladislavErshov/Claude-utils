# B8: cruise heap 4096 → 6144 (test-update-resize1) — FAILED до cruise

- **Cluster**: `9e0366c7-50da-4487-8746-d332357180d3` (test-update-resize1-mdbdev-kafka)
- **Operation**: `3b6c02f7-982f-4416-8fc1-acb87eaf1d23`
- **Запрос**: `/tmp/modify_B8_9e.json` — cruiseControl.jvmHeapSizeMb=6144, broker jvmHeapSizeMb=2048, socLogger.enabled=false
- **HTTP**: 202

## Temporal workflow

- `modifyKafkaCluster` 3b6c02f7 — FAILED
- `modifyController` — FAILED
- `updateControllerConfig` — FAILED на `1.controller.test-update-resize1-mdbdev-kafka.zc.one-infra.ru` (4-й хост, `HOST_NOT_READY`)
- `modifyCruise` / `updateCruiseConfig` — не запустились (упало раньше)

## PMS-результат

Cruise heap в PMS = **4096m** (не изменился) — workflow не дошёл до cruise-фазы.

## Особенность

На этом кластере в локальной БД `db_cluster` и `host_state` **нет записей** — modify стартовал, но в локальной БД нет хостов. Temporal workflow использует хосты из данных кластера в mdb-data, которые не были seeded локально.

## Выводы

1. Порядок workflow: `modifyController` → `modifyBroker` → `modifyCruise`. Если контроллер падает на 4-м хосте — cruise даже не запускается.
2. Для проверки cruise heap нужен кластер с минимум controller-хостами, которые могут «перезагрузиться» (dc/kc успешно, zc нет) — или делать PMS snapshot раньше, чем workflow упадёт.
