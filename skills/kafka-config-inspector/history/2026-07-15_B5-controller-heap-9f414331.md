# B5: controller heap resize 1024 → 1500 (test-sel-1)

- **Cluster**: `9f414331-8ec4-4102-b4c3-0c0bbf7b326d` (test-sel-1-mdbdev-kafka, project 160)
- **Operation**: `cbb7659e-0d01-42f1-9656-6a8f3638ab7b`
- **Запрос**: `/tmp/modify_B5_9f.json` — `controllerJvmHeapSizeMb: 1500` (baseline 1024), socLogger.enabled=true с topic=soc-audit-log-test-sel-1-C
- **HTTP**: 202

## Temporal workflow

- `modifyKafkaCluster` cbb7659e — FAILED (на 4-м хосте controller)
- `modifyController` — FAILED
- `updateControllerConfig` — FAILED на хосте `1.controller.test-sel-1-mdbdev-kafka.pc.one-infra.ru`
- Причина: `KafkaHostReloadHelper` не дождался готовности хоста (`HOST_NOT_READY`) — локально нет one-cloud API

## PMS-результат (ПОСЛЕ)

```bash
~/.claude/skills/kafka-cluster-inspector/bin/pms-read.sh 1.controller.test-sel-1-mdbdev-kafka.hc.one-infra.ru
```

`kafka.sysconfig` на controller-хостах:
- `KAFKA_HEAP_OPTS="-Xms1500m -Xmx1500m"` ✅ (было 1024m)

Контроллер на dc/kc успешно "restarted" (через PMS push + wait), pc — упал на reload.

## Выводы

1. Controller heap resize работает локально: PMS-переменная `kafka.sysconfig` обновляется до того, как workflow упадёт на `KafkaHostReloadHelper`.
2. **Один хост может успешно завершить reload** (видимо pms-propagation + ретраи KafkaHostWaiter), но для zc/pc — fail.
3. B5 — сценарий валиден для проверки PMS-записи controller heap.
