# Scenario G: cruise heap change (4096 → 2048)

- Cluster: 7569c837-37ba-4041-9046-92329683237e (test-resize)
- Date: 2026-07-15
- Modify: cruiseControl.jvmHeapSizeMb = 2048 (was null in DB, 4096 in PMS)
- Request: /tmp/modify_G.json
- Endpoint: PATCH http://localhost:8081/api/v2/mdb/kafka/clusters/{id}/modify
- Auth: JWT (serviceName=local-tester, projectId=160, accessType=w) signed with `mdb.auth.jwt.secret-key`

## Result
- HTTP 202 Accepted
- operation 65ce64a9-5ae3-40cd-88bf-3a23755757b9 in_progress
- version 137391 draft (cruise_heap=2048)

## Temporal workflows
- modifyKafkaCluster (RUNNING — main)
- modifyBroker (COMPLETED — skipped "no data")
- modifyController (COMPLETED — skipped "no data")
- modifyCruise (RUNNING)
  - updateCruiseConfig (RUNNING — висит на KafkaHostReloadHelper / HOST_NOT_READY)
  - resizeCruise (НЕ запустился — order=UPDATE_THEN_RESIZE)

## PMS changes
- ✅ kafka.cruisecontrol.properties: auto.rebalance, replication.throttle применены
- ⏳ kafka.cruisecontrol.sysconfig: KAFKA_HEAP_OPTS остался -Xms4096m (resize не отработал)
- ⏳ capacity.json: skipped (missing inputs: diskGb=null, lanInMb=null, lanOutMb=null)

## PMS host keys
- broker: test-resize-mdbdev-kafka.clouds
- controller: controller.test-resize-mdbdev-kafka.clouds
- cruise: test-resize-mdbdev-kafka.clouds (ДЕЛИТ с broker — важно!)

## Вывод
Backstage-часть (mdb-data → temporal workflow) отработала. Cruise heap change не виден в PMS, потому что resize-шаг ждёт завершения update-шага, который висит на HOST_NOT_READY (локально нет one-cloud API). Это ожидаемое поведение.
