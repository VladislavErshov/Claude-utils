# MDBSUP-5186 — Удалить Cruise из frontlogs-adtech-kafka (KC)

- Дата: 2026-09-08
- Кластер: `dbee9a79-1edf-41ca-b194-3f25cc1d425a` (frontlogs, Kafka, ns infra, облако KC, project 55)
- Хост: `1.cruise.frontlogs-adtech-kafka.kc.one-infra.ru`

## Суть

Кластер готовится к переезду в целевые ДЦ (TRG-229683). Просьба удалить Cruise Control;
затем автор создаст cruise заново в другом ДЦ с новым паролем (старый утерян).

## Выполнено

Полный повтор паттерна [MDBSUP-4827](../../../kafka-cluster-inspector/history/MDBSUP-4827-2026-08-25.md):
1. `mcc -c KC -n infra stop cruise.frontlogs-adtech-kafka` → FINISHED/STOPPED (~50 c).
2. `withdraw` сервиса — без уравнения-подтверждения, исчез за ~60 c (EntityNotFoundException).
3. `withdraw` storage `frontlogs-adtech-kafka.adtech.db.production.mdb.prod/cruise` —
   уравнение `9-3` через pexpect, state NORMAL→PURGEABLE→исчез (~80 c).
4. Прод-БД: `DELETE 1` host_state; `UPDATE 4` db_cluster_version (ключ `kafkaParams.cruiseControl`
   удалён; до правки значение было JSON null при существующем ключе). Верификация чистая.

## Отличия от 4827 / заметки

- В 4827 confirm-уравнение Storage withdraw подавали через `echo | mcc`; здесь echo не
  проверяли — сразу pexpect (см. `mcc-host-worker/commands/lifecycle.md`), сработало.
  Скрипт-обёртка: pexpect решает `N mod M` и `N op M`, лежит в tmp-сессии (паттерн в lifecycle.md).
- `one_cloud_meta` (cruise-control-service, fake_id 4278) не трогали — как в 4827.
- Перед началом: все операции кластера `done`, RUNNING-ретраев нет (последняя add_hosts 07.09).
