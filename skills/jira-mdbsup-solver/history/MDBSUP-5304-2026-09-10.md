# MDBSUP-5304 — logs: delete_hosts застрял на get_kafka_downscale_broker_result

Дата: 2026-09-10. Kafka, ns dzen, кластер `logs` (`f0d28ab1-bbd6-4288-a2f3-958b635a5f3e`),
очередь `logs-recom-wizard-kafka.recom-wizard.db.production.mdb.prod`.
Операция `9fc2e2fa-fa0f-4eda-862c-20f8989aba5f` (delete_hosts, брокер в hc, 12→11).

Таск оператора `kafka.downscale-broker` висел в precondition: `isFresh() false` — JMX (7777)
от всех ДЦ до uc-брокеров по внутренним (fd00) сетям отфильтрован (wan работает, Kafka 9092
по внутренним ходит). Рефреш сломан минимум с 08-09 (парковался и `kafka.update`).

Разобрано ручным downscale: op_stop → broker 20012 оказался уже пуст (данные кластера живут
только в uc/kc/pc) → unregister → mcc stop + rescale 11 → операция закрыта в done SQL.
Операцию will NOT self-converge: get_result закрыл её failed сразу после op_stop.

Полный разбор: [../../kafka-cluster-inspector/history/MDBSUP-5304-2026-09-10-downscale-hc-jmx-uc-blocked.md](../../kafka-cluster-inspector/history/MDBSUP-5304-2026-09-10-downscale-hc-jmx-uc-blocked.md)
