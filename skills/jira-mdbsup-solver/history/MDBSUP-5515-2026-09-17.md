# MDBSUP-5515 — 2026-09-17 — ручной сброс оффсетов dzen-visits (UI Gateway Timeout на большом кластере)

- **Кластер:** `kafka-tophits` (mytracker) `0516395f-bdf6-46a7-a8f1-0f70c23e2fab` — 67×3 брокеров (hc/kc/pc) + 3 контроллера
- **Суть:** UI mdb не открывает consumer group (Gateway Timeout — кластер слишком большой); пользователь просил сбросить оффсеты группы `dzen-visits` до latest (топик `tophits`, 360 партиций)
- **Диагностика:** группа была Stable (50 членов) → пользователь остановил консьюмеров → Empty, 0 members
- **Фикс:** CLI с брокера, auth `/opt/kafka/config/client.properties`: `--reset-offsets --topic tophits --to-latest` — dry-run, затем execute; 360/360, committed=LEO
- **Закрытие:** публичный комментарий + разбор для Developers (id 53329354), тикет закрыт
- **Хвост:** UI self-service reset на больших кластерах таймаутит — баг заведут отдельно позже
- Полный разбор: `kafka-cluster-inspector/history/MDBSUP-5515-2026-09-17-offset-reset-huge-cluster.md`
