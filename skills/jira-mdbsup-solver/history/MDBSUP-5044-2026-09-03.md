# MDBSUP-5044 (2026-09-03) — перегенерация SCRAM-пароля у продьюсера extdbpu

- **Кластер**: extdbpu (94bed650-7adf-4b78-9640-37a311033e65), 255 брокеров ec/kc/pc + 3 контроллера
- **Суть тикета**: продьюсер oneme-databus (UCP_ASYNC_MDB) не пишет в oneme_ucp_and_analytics_views —
  «Failed to update metadata after 10000 ms»
- **Корень (полный разбор — [kafka-cluster-inspector/history/MDBSUP-5044-2026-09-03-scram-producer-password.md](../../kafka-cluster-inspector/history/MDBSUP-5044-2026-09-03-scram-producer-password.md))**:
  mdb `update_user` 24.08 15:06 перегенерировал SCRAM-пароль `ucp_content_event_producer`
  (единственного writer топика); продьюсер упал при реконнекте 26.08 16:54 и циклится на
  invalid credentials. Фикс на стороне заявителя: взять актуальный пароль из mdb-data UI/vault
  → обновить конфиг UCP_ASYNC_MDB в PMS oneme → перезапустить продьюсер.
- **Паттерн**: «Failed to update metadata» + telnet 9092 проходит + в логах брокера
  `Failed authentication ... SCRAM-SHA-256` от одних и тех же IP на всех брокерах = креды клиента.
  Тот же юзер уже ломался 12-13.08 (INCALL-46719) — mdb update_user всегда перегенерирует пароль.
- **Попутная находка (не причина)**: фантомный hc-voter в KRaft кворуме — починен рестартами
  контроллеров, разбор — [kafka-cluster-inspector/history/MDBSUP-5044-2026-09-03-quorum-phantom-hc-voter.md](../../kafka-cluster-inspector/history/MDBSUP-5044-2026-09-03-quorum-phantom-hc-voter.md).
- ⚠️ Незакрытое (обновлено 08.09): завели нового юзера ucp_content_event_producer_check — фейлы
  остались. Live-тесты обеих пар: рабочие (старый юзер + l2DfG5tS3fR4 [перегенерён a.perevalov
  в 14:42], check + TmuBVrWwJ9ePZU7B); поды фейлятся с невалидной комбинацией и/или не
  рестартованы (креды читаются на старте, поды с 26.08). 08.09 ~16:20 заявителю в vk отправлен
  чек-лист (сверить креды в живом поде, kcat-тест, рестарт). Ожидаю рестарт → проверить логи
  брокеров на прекращение фейлов с их IP. Комментарий в Jira по раунду 2 не оставляли.
- ⚠️ Мина (с 31.08): перегенерён пароль oneme_yt_reader/oneme_yt_writer — их клиенты упадут
  при следующем реконнекте. Предупредить заявителя, отдельно от этого тикета.
