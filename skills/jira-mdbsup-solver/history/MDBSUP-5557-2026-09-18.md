# MDBSUP-5557 — qlab-production (mdb4922): удаление cruise после упавшего create_cluster (квота PC)

Дата: 2026-09-18. Kafka, ns infra, fullQueue `qlab-production-mdb4922-kafka.mdb4922.db.production.mdb.prod`.

create_cluster упал на `submit_cruise_control_manifest` (квота PC, MEM дефицит 4G) —
cruise-хост в облаке не создавался. Канон 4827/5263 в усечённом виде: withdraw не нужен
(EntityNotFound), только SQL (DELETE 1 host_state + UPDATE версии `#- cruiseControl` +
операция → done). Отличие от канона: после чистки оператор оставался not fresh —
`generate_kafka_operator_settings` остался scheduled и не записал PMS-свойства
(`queue://<fq>:root` = start.watcher=kafka.watch, `queue://<fq>:kafka` = 7 строк) —
дописаны руками через PMS update.do (host-one-cloud-ops/one-cloud-ops) + `mcc op_start
kafka.watch`. Через 5 минут — AVAILABLE, operator fresh.

Полный разбор: [../../kafka-cluster-inspector/history/MDBSUP-5557-2026-09-18-remove-cruise-quota-stuck-create.md](../../kafka-cluster-inspector/history/MDBSUP-5557-2026-09-18-remove-cruise-quota-stuck-create.md)
