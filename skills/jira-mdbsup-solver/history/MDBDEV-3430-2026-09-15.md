# MDBDEV-3430 — 2026-09-15

Таска: [kafka] переработать UI кнопки подключения (https://jira.vk.team/browse/MDBDEV-3430),
по аналогии с MDBDEV-2812 (redis connection-info + модалка). Команда NoSQL, связана Relates
с MDBDEV-3398 (ADR по проксе для работы с Kafka, автор — сам).

Контекст: кнопка «Подключиться» на Kafka-кластере сейчас просто выдаёт строку
(bootstrap.servers + SASL_SSL/SCRAM-SHA-256 + jaas с пустыми username/password) в кеш сохранения.

Сделано:
- Создана таска с описанием: отличие Kafka от Redis (после bootstrap клиент коннектится к
  advertised.listeners всех брокеров — одного tp-port-forward недостаточно), kafka-proxy
  (grepplabs) как способ локального подключения, ссылки на доку клиентов kafka-connect.html#klienty.
- Пользователь почистил описание (убрал endpoint connection-info в mdb-data и вариант
  /etc/hosts, оставил модалку + kafka-proxy + ссылки на доку телепорта). Дальнейшие правки —
  только поверх его версии, описание перечитывать перед update.
- Добавлен блок «Подключение к Cruise Control» (если заведён у кластера) из доки
  mdb-docs/docs/kafka/kafka-cruise-control.md, раздел «Подключение»: mcc tp-port-forward
  <FQDN CC>:443 --local-port <порт> --username <логин> (FQDN со страницы Хосты, суффикс wan
  убрать), https://127.0.0.1:<порт>, http basic auth логин `cruise` + пароль из создания
  Cruise, в меню выбрать кластер `dev`.

Уроки/факты:
- Репозиторий док: ~/Documents/Git/mdb-docs (docs/kafka/kafka-connect.md — подключение,
  Puncher/Teleport+HAProxy, сертификаты по неймспейсам, таблица клиентов; kafka-cruise-control.md —
  Cruise Control Web UI).
- В доке kafka-connect.md уже есть рецепт Teleport + HAProxy (SNI-маршрутизатор, разные
  локальные порты на брокеры + /etc/hosts) — модалка должна соответствовать доке.
