# MDBSUP-4923 — Удаление Cruise Control из HC, 2 кластера (vahter-mail-mini, workspace)

Дата: 2026-08-27. Kafka, ns infra, облако HC.

- vahter-mail-mini `9dacf209-8b81-4476-bce9-57ab44f1b191` (queue
  `vahter-mail-mini-mail-mini-kafka`, storage
  `vahter-mail-mini-mail-mini-kafka.mail-mini.db.production.mdb.prod/cruise`)
- workspace `9769ce4b-0e39-43ea-8994-9d79c81a440d` (queue `workspace-mail-kafka`,
  storage `workspace-mail-kafka.mail.db.production.mdb.prod/cruise`)

Порядок тот же, что в MDBSUP-4827 (stop → withdraw service → withdraw storage →
DELETE host_state + `jsonb - 'cruiseControl'`). DELETE 1 + UPDATE 5 на кластер,
верифицировано. one_cloud_meta не трогали. Отличия от 4827:

- Withdraw **сервиса** прошёл БЕЗ уравнения-подтверждения (только storage спрашивает).
- pexpect-regex из mcc-host-worker/commands/lifecycle.md не ловит `N mod M` — уравнение
  бывает `mod` (6 mod 3). Расширенный паттерн:
  `r"attempt 0/3\): (\d+)\s*(mod|[-+*/%])\s*(\d+)"` + ветка `ans = int(a) % int(b)`.
  Скрипт: /var/folders/.../opencode/withdraw.py (временный; паттерн перенесён в
  mcc-host-worker/commands/lifecycle.md).
- Имя storage = `<full service name без префикса cruise.>`; account-часть видна в
  `mcc status cruise.<queue>` (строка name: `cruise.<queue>.<account>`).

Полный канон: [../../kafka-cluster-inspector/history/MDBSUP-4827-2026-08-25.md](../../kafka-cluster-inspector/history/MDBSUP-4827-2026-08-25.md)

## Итог

Комментарий в Jira — в стиле MDBSUP-4918, но короче (2 абзаца: что выведено +
«добавление в EC — самостоятельно через UI»). Тикет закрыт (Closed). Добавление CC в
EC делает сам юзер через UI mdb-data (modify-флоу), не дежурный.

