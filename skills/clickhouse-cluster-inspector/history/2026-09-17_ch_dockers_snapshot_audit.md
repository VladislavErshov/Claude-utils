# 2026-09-17 — аудит снапшотов dockers всех CH-кластеров (по следам MDBSUP-5505)

Эталон `db_version_dockers`: CH 24.3 → service `ubuntu20-clickhouse-24.3.2.23:3.0.8`, keeper `...keeper-24.3.2.23:2.0.4`; CH 25.8 → service `ubuntu20-clickhouse-25.8:1.0.3`, keeper `...keeper-25.8:1.0.1`.

Метод: для каждого кластера бралась последняя `db_cluster_version` (по `create_ts`), из снапшота брался ПЕРВЫЙ тег по типу (для stat заморожен в workflow-инпут был именно первый; классификация ниже).

## Итог (868 кластеров)

- **180 OK** — первый тег = эталон
- **683 AT-RISK** — первый тег ≠ эталон (живость тега в dzen-registry не гарантирована; при следующем add_hosts/upscale тег заморозится в инпут — если тег вычищен, будет 404 как у stat)
- **5 LEGACY** — CH 22.3/22.10/24.8, образов в эталоне нет вообще

## Подтверждённо мёртвый тег: service `2.0.2` (404 в dzen-registry, MDBSUP-5505)

Первым в снапшоте — упадут так же, как stat:
- `logs-prod` (`c26cb42f`)
- `statistic-svc-prod` (`1e7177ae`)

Ещё 15 кластеров имеют 2.0.2 ВТОРЫМ тегом (первый 3.0.1/3.0.2/3.0.5) — если код сменится на выбор другого элемента массива, сломаются тоже; снапшоты с двойными записями — грязь в любом случае.

## Крупнейшие группы AT-RISK по тегам (svc/keep)

- 128: `3.0.1/2.0.2`, 115: `1.8.1/1.2.1`, 56: `3.0.5/2.0.2`, 41+26: 25.8 `1.0.0/...`, 39: `1.6.2/1.2.1`, 35: `3.0.2/2.0.2`, 30: `16.e52f.../prod`, 28: `1.2.0/1.0.1`, 22: `3.0.1/3.0.1`, 21: `prod/prod`, 19: `2.0.0/1.2.1`, хеш-теги 2024 г. (17+12+7+6+5+...), и мелочь.

Полные списки — в логе аудита (чат агента 2026-09-17).

## Лечение

Массовый идемпотентный UPDATE снапшотов на эталон (канон MDBSUP-5226, там 3014 строк Kafka): все записи service/keeper в `dockers` заменить на одну эталонную пару по версии кластера; legacy 22.x/24.8 не трогать. Убирает весь класс риска для будущих операций. НО: повторяется продуктовый баг — снапшоты не пересинхронизируются с `db_version_dockers` (MDBSUP-5226 → 5505), нужен фикс на стороне mdb-data/docker-images, иначе аудит придётся повторять после каждой чистки registry.

## Выполнено 2026-09-17 (массовый фикс)

- Верификация эталона перед фиксом: в `db_version_dockers` ровно 4 записи (24.3: `3.0.8`/`2.0.4`; 25.8: `1.0.3`/`1.0.1`, is_default), более новых тегов нет. `3.0.8` доказано жив (облако пропустило валидацию registry в stat 17.09); `1.0.3`/`1.0.1` подтверждены свежесозданными кластерами (raw 17.09 и др.); `2.0.4` — из того же релизного поезда, прямого теста не было.
- **Важная находка о механизме:** при создании новой строки версии dockers КОПИРУЮТСЯ из предыдущей строки кластера (свежие операции 17.09 у trend/poll/sellgate/pubhouse заморозили старые теги), а из `db_version_dockers` берутся только при создании самого кластера. Поэтому протухание вечное, а массовый фикс снапшотов — единственный способ.
- UPDATE: 1246 строк (24.3) + 467 строк (25.8), idемпотентно через `IS DISTINCT FROM` целевого jsonb. После фикса 868/868 кластеров на эталоне.
- **Грабля перехлёста:** 4 legacy-кластера (pubstat 22.10, infra-logs 24.8, devices001/spind 22.3) имели в снапшотах keeper-24.3 записи → попали под UPDATE, service-тег подменился на 24.3 (риск микса версий). Последние строки восстановлены в оригинальные legacy-пары вручную. Итог: 864 на эталоне + 4 legacy как были. Урок: при LIKE-фильтре по одному типу образов (keeper) переписывается ВЕСЬ массив вместе с service-записями других версий — legacy надо исключать ДО апдейта, по имени service-образа тоже.
- Тикет MDBSUP-5505 ведёт пользователь (ждёт квоту LAN out у продукта media-infra).
