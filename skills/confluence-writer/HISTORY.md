# История тасок confluence-writer

Журнал задач, выполненных с этим скиллом. Формат записи: дата, страница/объект, что сделано, замечания пользователя по стилю (они пополняют SKILL.md), технические уроки MCP.

---

## 2026-08-21 — «План действий для Kafka» (MDB, pageId=2126595641) + mdb-docs/fault-tolerance.md

**Задача.** Привести DRP-страницу Kafka в порядок по образцу сестринских страниц (ZooKeeper, ClickHouse), подробно описать min.insync.replicas, поправить парную пользовательскую доку в mdb-docs.

**Сделано в Confluence (v17 → v39):**
- Реструктуризация: панель «Связанные страницы», TOC, формальные заголовки, нумерованные шаги 1–6, манифесты Cruise Control — yaml-код-блоки с комментариями `# заменить`.
- Новый раздел «min.insync.replicas (min ISR) и доступность записи при отказе ДЦ»: принципы отказоустойчивости (два кворума), причины недоступности записи при -2 ДЦ, таблица возможностей/рисков, команды для дежурной смены (kafka-configs.sh / kafka-topics.sh), warning про возврат min ISR=2 и отражение в БД (kafka.sync).
- Все 5 inline-комментариев сохранены.

**Сделано в mdb-docs** (ветка `ershov/MDBDEV-3090-Add-info-about-min-ISR-for-Kafka-DRP`, не закоммичено на момент записи):
- fault-tolerance.md: раздел «Принципы обеспечения отказоустойчивости», таблица рисков, шаги разблокировки записи и возврата min ISR=2, UNCLEAN leader election — заранее через UI MDB, в инциденте только через MDBSUP.

**Замечания пользователя по стилю (перенесены в SKILL.md):**
1. Формальный стиль текста в Confluence и доке.
2. Заголовки должны быть понятными: «сейчас нифига по названию непонятно» — переименован раздел про min ISR.
3. Не дублировать ссылки (удалил дубль ссылки на доку).
4. Топология: штатно 3 контроллера, 5 — только аварийный режим при падении 2 контроллеров (сначала написал «обычно 5» — ошибка).

**Технические уроки MCP этой таски:**
- Англицизм «take from the manifest» проскочил в storage — выучено: перечитывать вставленный текст.
- Инкрементальные вставки секциями давали дубли и вложенные `<p>` в `<li>`; вылечено полной перезаписью body через `confluence_update_page`.
- 409-конфликт при двух параллельных `update_page_section` по одной странице — повтором вызова.
