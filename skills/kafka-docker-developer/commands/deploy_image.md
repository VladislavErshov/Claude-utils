# Деплой образа через CI / mdb modify (TODO)

⚠️ **Заглушка.** Заполнится по мере использования скилла.

## Scope

После того как образ собран в CI и запушен в registry, mdb-data должен его подтянуть на хосты
кластера. Это отдельный цикл от разработки — зависит от инфраструктуры mdb.

## Что задокументентировать когда понадобится

- Какой MR/триггер в `docker-images` запускает публикацию образа.
- Как mdb-data узнаёт про новый образ (version bump в PMS? mdb API?).
- Как запустить modify-флоу для обновления образа на хостах кластера.
- Связка с `/kafka-config-inspector` — сверка что после modify PMS-API значения применились.
- Откат на предыдущий образ.
